version: '3.8'

services:
  image-process:
    image: image-process:latest
    #container_name: image-process-service
    restart: always
    env_file:
      - image-process/.env

  camera-ingest:
    image: camera-ingest:latest
    container_name: camera-ingest-service
    ports:
      - '3000:3000'
    restart: always
    env_file:
      - camera-ingest/.env